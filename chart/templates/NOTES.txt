The app is deployed at
  {{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.tls }}s{{ end }}://{{ $host }}{{ include "presence.ingressPath" $ }}
  {{- end }}

You can confirm that its running by visiting
  {{- range $host := .Values.ingress.hosts }}
  http{{ if $.Values.tls }}s{{ end }}://{{ $host }}{{ include "presence.ingressPath" $ }}/healthz
  {{- end }}
or by running helm tests:
  helm test -n {{ .Release.Namespace }} {{ .Release.Name }} %chart_dir%

