apiVersion: v1
kind: ConfigMap
metadata:
  annotations: {}
  labels:
    {{- include "presence.labels" . | nindent 4 }}
  name: {{ include "presence.name" . }}-environment
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
